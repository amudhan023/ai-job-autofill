# API
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
python-multipart>=0.0.9

# AI (used in Phase 3; imported lazily so the app boots without keys)
anthropic>=0.40
# langgraph>=0.2          # enable when AI orchestration lands
# voyageai>=0.3           # embeddings (Anthropic-recommended)

# Resume parsing (Phase 3)
pdfminer.six>=20240706
python-docx>=1.1.2

# Observability (Phase 3)
# langsmith>=0.1
